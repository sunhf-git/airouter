{
  "name": "airouter",
  "version": "1.0.0",
  "description": "Cloudflare Worker reverse proxy for NVIDIA NIM, Google AI, and Groq APIs",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "build": "echo 'Build completed'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}